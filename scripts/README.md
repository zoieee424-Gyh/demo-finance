# Scripts

Planned scripts:

- `ingest_documents.py`: load financial documents into the knowledge base.
- `build_vector_index.py`: build or refresh local vector indexes.
- `run_eval.py`: run Agent/RAG quality evaluation cases.

