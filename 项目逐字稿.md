# 项目讲解视频逐字稿

本文档用于课程验收视频录制。建议视频时长控制在 8 到 12 分钟。录制时可以一边展示前端页面、README、流程图和 Git 日志，一边按本文稿讲解。

## 一、开场介绍

大家好，我这次介绍的项目是“智慧金融服务平台”，它是一个基于 Agentic RAG 与多智能体协作的金融服务系统。

这个项目面向五类金融业务场景：智能投顾、财报分析、风控审查、监管合规和金融科普。用户在前端输入一个金融问题后，系统会先判断这个问题属于哪类业务，再路由到对应的智能体。智能体会调用内部工具、检索知识库证据，并进行合规检查，最后生成结构化的金融分析报告。

项目的定位不是荐股系统，也不是自动交易系统，而是一个金融服务辅助平台。它强调分析、解释、风控、合规和证据可追溯。所有投资相关输出都必须带风险提示，不替用户做最终投资决策。

## 二、项目背景和业务价值

金融场景使用大模型有一个很明显的问题：如果直接让大模型自由回答，它可能会出现荐股、承诺收益、预测涨跌、编造依据等风险。

所以我这个项目没有采用“用户问，大模型直接答”的方式，而是采用了“DeepAgent 主脑 + 确定性工具 + RAG 证据 + 合规兜底”的架构。

在这个架构里，大模型主要负责理解用户问题、编排工具和组织报告；而风险等级、资产配置、合规判断、证据来源这些关键环节，尽量交给规则工具、知识库和合规模块来完成。

这样做的业务价值主要有三点：

第一，提高专业性。系统不是泛泛回答，而是按照不同金融业务场景调用专门的智能体。

第二，提高可解释性。前端会展示工具调用轨迹和证据来源，用户可以看到系统为什么得出这个结论。

第三，提高合规性。系统内置了合规审查，避免输出荐股、保证收益、目标价、必涨必跌等高风险内容。

## 三、技术栈说明

本项目的后端使用 FastAPI，负责提供接口、智能体调用、RAG 检索、SSE 推理链和合规校验。

前端使用 Vue3 和 Element Plus，提供五智能体统一工作台，支持手动选择智能体，也支持自动识别问题类型。

向量数据库使用 Chroma，用来存储知识库文本向量，实现语义检索。

数据库使用 MySQL，主要保存知识库的结构化元数据，例如 collection、document 和 chunk 信息。

Agent 和 RAG 核心采用 DeepAgent 加 LangChain/LangGraph 风格的工具编排。DeepAgent 作为主脑，负责决定调用哪些工具，以及如何组织报告。

大模型方面，项目通过 LLM Provider 层统一封装。当前演示使用 deepseek-v4-flash 云端 API。如果需要满足本地开源模型或免费 API 的要求，可以在 Provider 层替换为本地 Qwen、Llama 或其他免费 API，而不需要重写五个智能体和前端接口。

版本管理使用 Git，提交日志已经整理在 docs/git-log.md 中。

## 四、系统整体流程

下面我讲一下系统整体流程。

用户首先在前端输入问题。系统会把问题发送到后端的统一 Agent Router。

Router 会根据关键词、优先级和业务规则判断用户意图，比如是投顾问题、财报问题、风控问题、合规问题，还是金融科普问题。

确定意图后，系统会把请求交给对应智能体。智能体内部不是单纯调用一次大模型，而是由 DeepAgent 编排多个确定性工具。

同时，RAG 模块会根据问题构造检索请求，从 Chroma 知识库中检索相关证据。项目还预留了 BM25 稀疏检索、Hybrid 混合检索和 GraphRAG 图谱检索的扩展壳子。

智能体拿到工具结果和知识库证据后，会生成结构化报告。最后合规模块会检查报告内容，确保没有荐股、收益承诺、涨跌预测和替用户决策等问题。

前端最终展示报告正文、证据来源、工具调用轨迹和运行状态。

## 五、五个智能体说明

### 1. 智能投顾智能体

第一个智能体是智能投顾智能体。

它的业务目标是根据用户风险偏好、投资目标和资金规模，生成资产配置建议、基金定投方向、持仓诊断和市场热点解读。

它的流程是：先通过用户画像解析器提取用户资金、期限、风险偏好等信息；然后通过投资目标规划器判断用户是长期投资、教育金、养老，还是其他目标；接着通过风险评估器判断风险承受等级；然后资产配置引擎给出资产类别比例；基金定投规划器给出定投方向；如果用户提供持仓，还会调用持仓诊断器；最后通过 RAG 证据构建器和投顾合规审查器生成九章投顾报告。

这个智能体最重要的边界是：不推荐个股，不推荐具体基金代码，不承诺收益，不预测市场涨跌。

### 2. 财报分析智能体

第二个智能体是财报分析智能体。

它的业务目标是根据财报文本或财务指标，生成财务质量分析报告。

它会先通过财报文本解析器识别公司、期间和报表范围；然后通过财务指标提取器提取收入、利润、现金流、负债等数据；接着分别分析盈利能力、偿债能力、成长效率和异常风险；最后结合 RAG 证据和财报合规审查器，生成八章财报分析报告。

这个智能体的定位是财务解读，不是投资推荐。它可以说明企业财务质量和风险点，但不能输出目标价、买入建议或卖出建议。

### 3. 风控审查智能体

第三个智能体是风控审查智能体。

它的业务目标是对投资组合、产品材料或用户风险画像进行风险检查。

它会先解析用户输入中的资产、金额、期限和风险偏好；然后分别调用集中度风险检查器、流动性风险评估器、财务质量风险检测器和风险偏好匹配器；接着由风险缓释规划器给出降低风险的方向；最后结合 RAG 证据和风控合规审查器，生成风控审查报告。

它的定位是风险管理助手，不是交易决策器。所以它会给出“建议关注、建议降低集中度、建议补充流动性”等审慎表达，而不是直接让用户买入或卖出。

### 4. 监管合规智能体

第四个智能体是监管合规智能体。

它主要用于审查金融营销话术、投顾文本和产品宣传材料。

它会先解析待审查文本，然后调用禁止性表达检测器，识别保证收益、必涨、目标价、推荐买入等高风险表达；再调用适当性风险检查器和风险披露完整性检查器，判断是否忽略用户风险承受能力，是否缺少风险提示；之后通过监管依据匹配器找到相关合规依据，再由合规改写规划器给出替代表述。

最后它会输出八章监管合规报告，告诉用户哪里违规、为什么违规、应该怎么改。

这个智能体在演示时很直观，比如输入“保证年化收益 8%，稳赚不赔”，系统会识别这是高风险违规表达，并给出更合规的改写方式。

### 5. 金融科普智能体

第五个智能体是金融科普智能体。

它面向普通用户，用来解释基金、债券、指数、净值、防诈骗、产品区别等金融知识。

它会先通过学习者画像分析器判断用户基础，然后判断问题类型。如果是简单概念解释，会走一个复合工作流工具，一次性完成画像、概念解释、证据构建和合规检查，这样可以减少大模型重复调用，提高响应速度。

如果是防诈骗问题，会调用诈骗风险识别器；如果是产品比较，会调用产品知识映射器；如果是学习路径，会调用学习路径规划器。

最终它会生成适合普通用户理解的金融科普报告。它只做知识解释，不输出具体投资建议。

## 六、RAG、BM25、Hybrid 和 GraphRAG 说明

本项目的 RAG 当前主路径是 Chroma 语义检索。知识库文本会被切分、向量化并写入 Chroma。用户提问时，系统根据业务意图选择对应知识库集合，然后检索相关资料，形成 Evidence Pack，注入到智能体报告中。

MySQL 用来保存知识库结构化信息，比如集合、文档、chunk 和哈希信息。Chroma 负责向量检索，MySQL 负责元数据管理。

项目中还预留了 BM25 稀疏检索和 Hybrid 混合检索。BM25 适合关键词精确匹配，Hybrid 可以把语义检索和关键词检索结合起来。当前 BM25 是架构壳子，Hybrid 当前近似等于语义检索，后续可以接入真实 BM25 排序和 RRF 融合。

项目还预留了 GraphRAG 壳子。GraphRAG 适合处理关系型金融问题，比如公司和风险因子之间的关系、法规和违规表达之间的关系、财务指标和报表章节之间的关系、金融概念之间的学习路径关系。

普通向量检索擅长找语义相似文本，而 GraphRAG 更擅长沿着实体、关系和路径找证据。当前项目已经提供 GraphRAGRetriever 和配置开关，后续可以接 MySQL 图关系表、NetworkX、Neo4j 或 NebulaGraph。

## 七、前端演示流程

接下来可以演示前端页面。

首先双击 start_finance_agent.bat，一键启动后端和前端。后端默认运行在 127.0.0.1:8010，前端运行在 127.0.0.1:5173。

进入页面后，可以看到左侧有五个智能体，也可以选择自动识别模式。

第一个演示问题可以输入：

“我有 20 万资金，风险偏好中等，希望做 3 到 5 年长期投资，应该怎么配置？”

这个问题会进入智能投顾场景。前端会展示推理链，比如用户画像解析、投资目标规划、风险评估、资产配置、合规审查和证据构建。最后页面会展示一份结构化投顾报告。

第二个演示问题可以输入：

“这段宣传语能不能用：本产品保证年化收益 8%，稳赚不赔，建议立即买入。”

这个问题会进入监管合规场景。系统会识别保证收益、稳赚不赔、建议立即买入等违规风险，并给出更合规的替代表述。

第三个演示问题可以输入：

“什么是指数基金？适合新手理解的方式解释一下。”

这个问题会进入金融科普场景。系统会用更通俗的语言解释概念，并展示相关证据来源。

演示时重点展示三个区域：第一是报告正文，第二是工具调用轨迹，第三是证据来源。这样可以体现这个项目不是普通聊天机器人，而是有流程、有工具、有证据的 Agentic RAG 系统。

## 八、Git 和提交材料说明

项目已经提交到 GitHub，并使用 Git 做版本管理。

可以展示命令：

```bash
git log --oneline --graph --decorate --all
```

提交日志可以看到项目从启动脚本和文档，到后端 DeepAgent 智能体，再到 Vue 前端、知识库文档、课程提交说明、GraphRAG 壳子、Python 环境依赖和智能体流程图的完整开发过程。

Git 日志说明文档放在 docs/git-log.md。

五个智能体流程图放在项目主目录的《智能体流程图.md》。

GraphRAG 壳子说明放在 docs/graphrag.md。

完整 Python 环境依赖导出在 backend/requirements-lock.txt。

## 九、项目总结

总结一下，这个项目完成了一个智慧金融服务平台的 MVP 闭环。

从业务上看，它覆盖了智能投顾、财报分析、风控审查、监管合规和金融科普五类金融场景。

从技术上看，它使用 FastAPI、Vue3、Element Plus、Chroma、MySQL、DeepAgent、RAG 和 Git，满足课程技术栈要求。

从架构上看，它不是简单的大模型问答，而是 DeepAgent 主脑编排多个确定性工具，并结合 RAG 证据和合规审查，生成更可解释、更安全的金融报告。

从演示上看，前端可以展示报告、证据来源和推理链，一键启动脚本也方便本地运行。

后续如果继续优化，我会重点做三件事：第一，完善真实 BM25 和 Hybrid 融合排序；第二，接入真实 GraphRAG 图谱检索；第三，继续优化前端交互和报告导出能力。

## 十、3 分钟精简版讲稿

如果视频时间比较短，可以使用下面这个版本。

大家好，我介绍的项目是“智慧金融服务平台”，它是一个基于 Agentic RAG 和多智能体协作的金融服务系统。

这个项目覆盖五个业务场景：智能投顾、财报分析、风控审查、监管合规和金融科普。用户输入问题后，系统会先通过 Agent Router 判断问题类型，再交给对应智能体处理。

项目的核心架构是 DeepAgent 主脑加确定性工具加 RAG 证据加合规兜底。大模型负责理解问题和组织报告，关键金融判断由工具和知识库完成，最后再通过合规模块检查，避免荐股、承诺收益、预测涨跌和替用户决策。

技术栈方面，后端使用 FastAPI，前端使用 Vue3 和 Element Plus，向量数据库使用 Chroma，结构化元数据使用 MySQL，Agent 和 RAG 使用 DeepAgent 与 LangChain/LangGraph 风格工具编排。模型通过 Provider 层封装，当前演示使用 deepseek-v4-flash，也可以替换成本地 Qwen、Llama 或免费 API。

五个智能体中，智能投顾负责资产配置和定投建议，但不推荐个股；财报分析负责解读财务指标，但不输出买卖建议；风控审查负责识别集中度、流动性、财务质量等风险；监管合规负责检查保证收益、荐股话术、风险披露不足等问题；金融科普负责解释基金、债券、指数和防诈骗知识。

RAG 部分目前主路径是 Chroma 语义检索，MySQL 保存知识库元数据。项目还预留了 BM25、Hybrid 和 GraphRAG 壳子，后续可以增强关键词检索、混合排序和图谱关系检索。

前端页面支持五个智能体切换和自动识别，能展示报告、证据来源和推理链。项目也提供了一键启动脚本 start_finance_agent.bat，方便本地演示。

最后，这个项目不是股票推荐系统，也不是自动交易系统，而是一个强调分析、解释、风控和合规的金融服务辅助平台。
